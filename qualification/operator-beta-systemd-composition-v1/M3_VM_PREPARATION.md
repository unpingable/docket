# M3 governed VM candidate preparation

Source preparation only; VM NOT_RUN. Integration owner is root. This isolated
derivative starts from modern baseline producer1d6cb0b, whose ordinary composition
package uses Docket09ba (runtime identical to152) and NQ920. It does not inherit
M2/M4 qualification. Production access remains separately blocked.

`m3_vm_inputs.py` fixes the seven accepted package/receipt/wheel identities, exact
application17a, NQ runtime920 and modern NQ harness e0151d0. Driver package8b2e7809
has independent package and native-test acceptance; NQ packagebb9b89fb likewise.
Host-debug binaryfb1e is not installed into guests; packaged nq370c4fdf is required.

`m3_guest_matrix.py` sequences the existing12 stage and49 later variants exactly
once, then the separately supplied frozen checker, then existing scoped teardown.
Each case has retained STARTED, command streams/exit, checked result and completion.
Failure/timeout stops the batch with a retained REFUSAL; no retries, resumed effects
or silent teardown. The actual foreign-operation donor is separately checked and
archived/stopped. Checker code and its imports must be enrolled root-owned immutable
as a separate B tree; checking its main-script digest does not replace whole-tree
source custody. Guest setup's protected-root premise remains explicit.

## Dependency/order and remaining source work

1. Root reviews this fixed input/batch contract and freezes producer A/checker B.
2. Add the thin host adapter using existing NQ VM lifecycle and ordinary composition
   baseline. Run M3 batch after baseline evidence, before bounded teardown. Do not
   call the inherited M4 day-two/cold hooks: those are a separate campaign.
3. Enroll exact app archive, A/B source trees, universal wheel and driver package;
   retain/recheck copied hashes. Use existing package-install and SSH mechanisms.
4. Retain full stopped-case archives and checker records before guest destruction.
   A failed case leaves its exact guest state for bounded reconciliation; no rerun.
5. Give M3 its own terminal disposition and host manifest/teardown checks. Baseline
   success, helper completion and completed transport do not establish M3 success.
6. Independent source review, preflight-only checks, then root-coordinated launch.

No new daemon, persistence authority or orchestration substrate is required. The
batch is a finite qualification script around existing AG/Docket helper units.
An actual lost companion supervisor remains a separate required execution case;
helper exit77 alone does not qualify controller-loss recovery.

## Resource/launch proposal — not authorization

Proposed ports23201/23202 TCP and24617 UDP, distinct from M4's23181/23182/24597.
Root must still check availability and prohibit conflicting VM runs. Reuse existing
two-role guest lifecycle/images and memory settings; reserve24GiB scratch estimate
plus global24GiB free floor. Sequential cases use bounded4MiB target/8MiB backup
tmpfs; retained archives and guests consume additional measured disk. These are
planning reservations, not established peaks. Per-case producer timeout900s,
checker/teardown90s, finite7200s case-launch budget; no production timing claim.
No VM, production read, deployment or package installation occurred in this pass.
